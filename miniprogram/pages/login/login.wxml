<view class="login-container">
  <view class="login-header">
    <view class="logo">📚</view>
    <view class="title">源来是糖</view>
    <view class="subtitle">每天坚持，养成好习惯</view>
  </view>

  <view class="login-card">
    <view class="welcome-title">欢迎使用</view>
    <view class="welcome-desc">使用微信账号快速登录，记录孩子的每一次成长</view>

    <button class="btn-wechat" bindtap="handleWxLogin" loading="{{loading}}">
      <text class="wx-icon">💬</text>
      <text>微信一键登录</text>
    </button>

    <button class="btn-skip" bindtap="goBack">
      先逛逛，看看活动
    </button>

    <view class="agreement" bindtap="toggleAgree">
      <view class="checkbox {{agreed ? 'checked' : ''}}">
        <text wx:if="{{agreed}}">✓</text>
      </view>
      <view class="agreement-text">
        <text>我已阅读并同意</text>
        <text class="link" catchtap="goToPrivacy">《用户隐私保护指引》</text>
      </view>
    </view>
  </view>

  <view class="footer-tip">
    <text>💡 登录后可在「我的」设置昵称和孩子名字</text>
  </view>

  <!-- 昵称设置弹窗 -->
  <view class="nickname-overlay" wx:if="{{showNicknameSetup}}">
    <view class="nickname-modal">
      <view class="nickname-title">完善一下资料</view>
      <view class="nickname-desc">选择头像并设置昵称</view>

      <button
        class="avatar-picker"
        open-type="chooseAvatar"
        bind:chooseavatar="onChooseAvatar"
      >
        <image
          wx:if="{{setupAvatarUrl}}"
          class="avatar-picker-img"
          src="{{setupAvatarUrl}}"
          mode="aspectFill"
        />
        <view wx:else class="avatar-placeholder">
          <text class="avatar-placeholder-icon">+</text>
          <text class="avatar-placeholder-text">选择头像</text>
        </view>
      </button>

      <input
        type="nickname"
        class="nickname-input"
        placeholder-class="input-placeholder"
        placeholder="输入你的昵称"
        value="{{setupNickname}}"
        bindinput="onNicknameInput"
        bindblur="onNicknameInput"
        bindnicknamereview="onNicknameReview"
        maxlength="20"
      />
      <view class="nickname-actions">
        <button class="btn-save" bindtap="saveNickname" loading="{{saving}}">保存</button>
      </view>
    </view>
  </view>

  <!-- 孩子名称设置弹窗 -->
  <view class="nickname-overlay" wx:if="{{showChildSetup}}">
    <view class="nickname-modal">
      <view class="nickname-title">孩子怎么称呼？</view>
      <view class="nickname-desc">打卡记录会显示孩子的名字</view>
      <input
        class="nickname-input"
        placeholder-class="input-placeholder"
        placeholder="如：糖糖、小明"
        value="{{setupChildName}}"
        bindinput="onChildNameInput"
        bindblur="onChildNameInput"
        maxlength="20"
      />
      <view class="nickname-actions">
        <button class="btn-save" bindtap="saveChildName" loading="{{saving}}">保存，开始打卡</button>
      </view>
    </view>
  </view>
</view>
