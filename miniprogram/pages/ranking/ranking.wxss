.container {
  padding-bottom: 180rpx;
}

.event-switcher {
  position: relative;
  z-index: 2;
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 20rpx;
  padding: 12rpx 28rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.14);
  border: 1rpx solid rgba(255, 255, 255, 0.22);
  max-width: 100%;
}

.event-switcher-name {
  color: #fff;
  font-size: 26rpx;
  font-weight: 600;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 460rpx;
}

.event-switcher-arrow {
  color: rgba(255, 255, 255, 0.85);
  font-size: 22rpx;
}

.ranking-header {
  background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
  margin: -32rpx -28rpx 0;
  padding: 56rpx 40rpx 130rpx;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.ranking-header::before {
  content: '';
  position: absolute;
  top: -100rpx;
  right: -60rpx;
  width: 280rpx;
  height: 280rpx;
  background: rgba(255, 255, 255, 0.08);
  border-radius: 50%;
}

.ranking-header::after {
  content: '';
  position: absolute;
  bottom: -80rpx;
  left: 10%;
  width: 200rpx;
  height: 200rpx;
  background: rgba(255, 255, 255, 0.06);
  border-radius: 50%;
}

.ranking-header-title {
  position: relative;
  z-index: 1;
  font-size: 42rpx;
  font-weight: 800;
  color: var(--text);
  text-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.2);
}

.ranking-header-subtitle {
  position: relative;
  z-index: 1;
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.8);
  margin-top: 12rpx;
  font-weight: 500;
}

.my-rank-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  background: var(--surface);
  border-radius: 28rpx;
  padding: 28rpx 32rpx;
  margin: -84rpx 0 28rpx;
  position: relative;
  z-index: 12;
  box-shadow: 0 12rpx 36rpx rgba(0, 0, 0, 0.28);
  border: 2rpx solid var(--border);
  animation: rank-fade-up 0.4s ease-out both;
}

.my-rank-main {
  display: flex;
  align-items: center;
  gap: 22rpx;
  min-width: 0;
  flex: 1;
}

.my-rank-avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  color: var(--text);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 34rpx;
  font-weight: 600;
  flex-shrink: 0;
  box-shadow: 0 6rpx 20rpx var(--primary-glow);
}

image.my-rank-avatar {
  display: block;
}

.my-rank-body {
  min-width: 0;
  flex: 1;
}

.my-rank-label {
  color: var(--text-muted);
  font-size: 24rpx;
  font-weight: 600;
}

.my-rank-title {
  color: var(--text);
  font-size: 40rpx;
  line-height: 1.25;
  font-weight: 800;
  margin-top: 6rpx;
}

.my-rank-subtitle {
  color: var(--text-muted);
  font-size: 24rpx;
  margin-top: 8rpx;
}

.my-rank-gap {
  padding: 0 20rpx;
  height: 64rpx;
  border-radius: 999rpx;
  background: var(--warning-soft);
  color: var(--warning);
  font-size: 26rpx;
  font-weight: 700;
  white-space: nowrap;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1rpx solid rgba(251, 191, 36, 0.2);
  flex-shrink: 0;
}

.podium {
  display: flex;
  justify-content: center;
  align-items: flex-end;
  gap: 20rpx;
  margin: 0 0 32rpx;
  padding-top: 24rpx;
  position: relative;
  z-index: 10;
  animation: rank-fade-up 0.4s ease-out 0.08s both;
}

.podium-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  background: var(--surface);
  border-radius: 28rpx;
  padding: 28rpx 12rpx 24rpx;
  box-shadow: 0 12rpx 40rpx rgba(0, 0, 0, 0.28);
  position: relative;
  border: 2rpx solid var(--border);
}

.podium-item.first {
  padding: 44rpx 12rpx 30rpx;
  z-index: 2;
  background: linear-gradient(180deg, rgba(251, 191, 36, 0.12) 0%, var(--surface) 60%);
  box-shadow: 0 16rpx 48rpx rgba(251, 191, 36, 0.15);
  border-color: rgba(251, 191, 36, 0.25);
}

.podium-item.second {
  background: linear-gradient(180deg, rgba(148, 163, 184, 0.1) 0%, var(--surface) 60%);
}

.podium-item.third {
  background: linear-gradient(180deg, rgba(249, 115, 22, 0.1) 0%, var(--surface) 60%);
}

.podium-crown {
  margin-bottom: 6rpx;
  will-change: transform;
  animation: bounce 2s ease-in-out infinite;
}

.crown-icon {
  font-size: 40rpx;
  color: var(--warning);
  text-shadow: 0 0 12rpx rgba(251, 191, 36, 0.5);
}

@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8rpx); }
}

@keyframes rank-fade-up {
  from {
    opacity: 0;
    transform: translateY(24rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.podium-rank {
  width: 52rpx;
  height: 52rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 800;
  margin-bottom: 10rpx;
  color: var(--text);
}

.podium-rank.rank-1 {
  background: rgba(251, 191, 36, 0.22);
  color: #fcd34d;
}

.podium-rank.rank-2 {
  background: rgba(148, 163, 184, 0.18);
  color: #e2e8f0;
}

.podium-rank.rank-3 {
  background: rgba(249, 115, 22, 0.16);
  color: #fdba74;
}

.podium-avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  color: var(--text);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  font-weight: 600;
  margin-bottom: 14rpx;
  box-shadow: 0 6rpx 20rpx var(--primary-glow);
}

.podium-avatar.gold {
  background: linear-gradient(135deg, #fbbf24, #f59e0b);
  box-shadow: 0 6rpx 20rpx rgba(245, 158, 11, 0.3);
  width: 104rpx;
  height: 104rpx;
  font-size: 40rpx;
}

.podium-avatar.bronze {
  background: linear-gradient(135deg, #fb923c, #ea580c);
  box-shadow: 0 6rpx 20rpx rgba(249, 115, 22, 0.25);
}

image.podium-avatar {
  display: block;
}

.podium-name {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 6rpx;
  text-align: center;
  width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.podium-days {
  font-size: 26rpx;
  color: var(--primary);
  font-weight: 700;
}

.podium-item.first .podium-days {
  font-size: 28rpx;
  color: var(--warning);
}

.list-section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text);
  margin: 0 4rpx 20rpx;
  padding-left: 16rpx;
  border-left: 6rpx solid var(--primary);
  border-radius: 4rpx;
}

.ranking-list {
  padding: 0;
  overflow: hidden;
  border-radius: 28rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.24);
  background: var(--surface);
  border: 2rpx solid var(--border);
}

.ranking-skeleton .ranking-item:last-child {
  border-bottom: none;
}

.ranking-item {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 28rpx 32rpx;
  border-bottom: 2rpx solid var(--border);
  transition: background 0.2s;
}

.ranking-item:last-child {
  border-bottom: none;
}

.ranking-item.is-me {
  background: linear-gradient(90deg, var(--primary-soft) 0%, var(--surface) 100%);
}

.rank-num {
  width: 48rpx;
  text-align: center;
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-muted);
  flex-shrink: 0;
}

.rank-num.rank-top-1 {
  color: var(--warning);
  text-shadow: 0 0 12rpx rgba(251, 191, 36, 0.35);
}

.rank-num.rank-top-2 {
  color: #e2e8f0;
}

.rank-num.rank-top-3 {
  color: #fdba74;
}

.user-avatar {
  width: 76rpx;
  height: 76rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  color: var(--text);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30rpx;
  font-weight: 600;
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx var(--primary-glow);
}

image.user-avatar {
  display: block;
}

.user-info {
  min-width: 0;
  flex: 1;
}

.user-name {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text);
  display: flex;
  align-items: center;
  min-width: 0;
  margin-bottom: 6rpx;
}

.user-name-text {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  padding: 2rpx 12rpx;
  border-radius: 999rpx;
  font-size: 20rpx;
  font-weight: 700;
  margin-left: 10rpx;
}

.badge-rose {
  background: var(--primary-soft);
  color: var(--primary);
}

.streak-tag {
  font-size: 22rpx;
  color: var(--warning);
  font-weight: 500;
}

.user-days {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--primary);
  flex-shrink: 0;
}
