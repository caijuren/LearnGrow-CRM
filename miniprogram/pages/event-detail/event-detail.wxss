.container {
  padding-bottom: 180rpx;
}

.event-hero {
  background: linear-gradient(135deg, #F43F7A 0%, #8B5CF6 100%);
  margin: -32rpx -28rpx 32rpx;
  padding: 80rpx 40rpx 56rpx;
  border-radius: 0 0 40rpx 40rpx;
  position: relative;
  overflow: hidden;
}

.event-hero::before {
  content: '';
  position: absolute;
  top: -80rpx;
  right: -80rpx;
  width: 240rpx;
  height: 240rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
}

.event-hero::after {
  content: '';
  position: absolute;
  bottom: -60rpx;
  left: 20%;
  width: 160rpx;
  height: 160rpx;
  background: rgba(255, 255, 255, 0.08);
  border-radius: 50%;
}

.hero-title {
  line-height: 1.4;
  font-size: 44rpx;
  position: relative;
  z-index: 1;
  color: white;
  text-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.2);
}

.hero-meta {
  font-size: 26rpx;
  position: relative;
  z-index: 1;
  color: rgba(255, 255, 255, 0.9);
}

.hero-badges {
  flex-wrap: wrap;
  position: relative;
  z-index: 1;
}

.hero-badge {
  background: rgba(255, 255, 255, 0.2);
  color: white;
  padding: 10rpx 24rpx;
  border-radius: 24rpx;
  font-size: 24rpx;
  backdrop-filter: blur(10px);
  border: 1rpx solid rgba(255, 255, 255, 0.15);
}

.section-mark {
  width: 42rpx;
  height: 42rpx;
  border-radius: 12rpx;
  background: rgba(244, 63, 122, 0.12);
  color: #F43F7A;
  font-size: 24rpx;
  font-weight: 800;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.reward-card {
  background: linear-gradient(135deg, rgba(251, 191, 36, 0.12) 0%, rgba(245, 158, 11, 0.08) 100%);
  border: 2rpx solid rgba(251, 191, 36, 0.2);
  position: relative;
  overflow: hidden;
}

.reward-card::before {
  content: '🏆';
  position: absolute;
  right: -10rpx;
  top: -10rpx;
  font-size: 120rpx;
  opacity: 0.12;
}

.checked-status {
  text-align: center;
  padding: 48rpx 32rpx;
}

.check-icon {
  width: 140rpx;
  height: 140rpx;
  background: linear-gradient(135deg, rgba(52, 211, 153, 0.15), rgba(52, 211, 153, 0.08));
  color: #34d399;
  border-radius: 50%;
  font-size: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto;
  box-shadow: 0 8rpx 24rpx rgba(52, 211, 153, 0.15), 0 0 0 1rpx rgba(52, 211, 153, 0.2);
}

.check-icon.pending {
  background: linear-gradient(135deg, rgba(251, 191, 36, 0.15), rgba(251, 191, 36, 0.08));
  color: #fbbf24;
  box-shadow: 0 8rpx 24rpx rgba(251, 191, 36, 0.15), 0 0 0 1rpx rgba(251, 191, 36, 0.2);
}

.note-text {
  background: #090B12;
  padding: 24rpx;
  border-radius: 16rpx;
  color: #94a3b8;
  text-align: left;
  line-height: 1.6;
  border: 2rpx solid #1F2937;
}

.checkin-image {
  width: 100%;
  max-height: 400rpx;
  border-radius: 16rpx;
}

.checkin-video {
  width: 100%;
  height: 400rpx;
  border-radius: 16rpx;
  background: #090B12;
}

.form-item {
  margin-bottom: 32rpx;
}

.label {
  font-size: 28rpx;
  color: #f8fafc;
  margin-bottom: 16rpx;
  font-weight: 600;
}

.textarea {
  width: 100%;
  min-height: 180rpx;
  background: #090B12;
  border-radius: 20rpx;
  padding: 24rpx;
  font-size: 28rpx;
  box-sizing: border-box;
  border: 2rpx solid #1F2937;
  line-height: 1.6;
  color: #f8fafc;
}

.textarea:focus {
  border-color: #F43F7A;
  box-shadow: 0 0 0 4rpx rgba(244, 63, 122, 0.1);
}

.input {
  width: 100%;
  height: 88rpx;
  background: #090B12;
  border-radius: 20rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  box-sizing: border-box;
  border: 2rpx solid #1F2937;
  color: #f8fafc;
}

.input:focus {
  border-color: #F43F7A;
  box-shadow: 0 0 0 4rpx rgba(244, 63, 122, 0.1);
}

.upload-area {
  width: 200rpx;
  height: 200rpx;
  background: #090B12;
  border: 2rpx dashed rgba(244, 63, 122, 0.35);
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  transition: all 0.2s;
}

.upload-area-empty {
  background: rgba(244, 63, 122, 0.04);
}

.upload-area-empty:active {
  border-color: rgba(244, 63, 122, 0.7);
  background: rgba(244, 63, 122, 0.08);
}

.preview-image {
  width: 100%;
  height: 100%;
}

.preview-video {
  width: 100%;
  height: 100%;
  background: #000;
}

.upload-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  color: rgba(255, 255, 255, 0.45);
}

.upload-icon {
  width: 64rpx;
  height: 64rpx;
  border-radius: 16rpx;
  background: rgba(244, 63, 122, 0.12);
  color: #F43F7A;
  font-size: 36rpx;
  font-weight: 300;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 12rpx;
}

.makeup-banner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
  padding: 22rpx 24rpx;
  margin-bottom: 28rpx;
  border-radius: 18rpx;
  background: rgba(59, 130, 246, 0.1);
  border: 2rpx solid rgba(59, 130, 246, 0.2);
}

.makeup-banner-title {
  color: #60a5fa;
  font-size: 28rpx;
  font-weight: 700;
}

.edit-banner {
  position: relative;
  padding: 22rpx 24rpx;
  margin-bottom: 28rpx;
  border-radius: 18rpx;
  background: rgba(244, 63, 122, 0.08);
  border: 2rpx solid rgba(244, 63, 122, 0.2);
}

.edit-banner-title {
  color: #F43F7A;
  font-size: 28rpx;
  font-weight: 700;
}

.edit-banner-desc {
  color: rgba(255, 255, 255, 0.5);
  font-size: 24rpx;
  margin-top: 6rpx;
}

.edit-banner-close {
  position: absolute;
  right: 24rpx;
  top: 50%;
  transform: translateY(-50%);
  color: #F43F7A;
  font-size: 26rpx;
  padding: 10rpx 16rpx;
}

.makeup-banner-desc {
  color: #94a3b8;
  font-size: 24rpx;
  margin-top: 6rpx;
}

.makeup-clear {
  margin: 0;
  padding: 10rpx 20rpx;
  border-radius: 14rpx;
  background: #111522;
  color: #60a5fa;
  font-size: 24rpx;
  line-height: 1.4;
  border: 1rpx solid rgba(59, 130, 246, 0.2);
}

.makeup-clear::after {
  border: none;
}

.reject-banner {
  padding: 22rpx 24rpx;
  margin-bottom: 28rpx;
  border-radius: 18rpx;
  background: rgba(239, 68, 68, 0.1);
  border: 2rpx solid rgba(239, 68, 68, 0.2);
}

.reject-title {
  color: #f87171;
  font-size: 28rpx;
  font-weight: 700;
}

.reject-desc {
  color: #94a3b8;
  font-size: 24rpx;
  margin-top: 6rpx;
  line-height: 1.5;
}

.streak-info {
  justify-content: space-around;
  padding: 28rpx 24rpx;
  background: #090B12;
  border-radius: 20rpx;
  margin-bottom: 28rpx;
  border: 2rpx solid #1F2937;
}

.streak-item {
  text-align: center;
  flex: 1;
}

.streak-num {
  font-size: 52rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #F43F7A, #8B5CF6);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.streak-item:first-child .streak-num {
  background: linear-gradient(135deg, #fbbf24, #f97316);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.streak-label {
  font-size: 24rpx;
  color: #64748b;
  margin-top: 8rpx;
}

.calendar-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 10rpx;
}

.calendar-weekday {
  text-align: center;
  font-size: 24rpx;
  color: #64748b;
  padding: 16rpx 0 12rpx;
  font-weight: 500;
}

.calendar-day {
  aspect-ratio: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  border-radius: 14rpx;
  color: #94a3b8;
  font-weight: 500;
  transition: all 0.2s;
  position: relative;
  flex-direction: column;
  background: #090B12;
}

.calendar-day.checked {
  background: linear-gradient(135deg, #F43F7A, #ec4899);
  color: white;
  font-weight: 600;
  box-shadow: 0 4rpx 12rpx rgba(244, 63, 122, 0.25);
}

.calendar-day.today {
  border: 2rpx solid #F43F7A;
  color: #F43F7A;
}

.calendar-day.today.checked {
  border: none;
  color: white;
}

.calendar-day.pending {
  background: rgba(251, 191, 36, 0.12);
  color: #fbbf24;
}

.calendar-day.rejected,
.calendar-day.missed {
  background: rgba(239, 68, 68, 0.12);
  color: #f87171;
}

.calendar-day.can-makeup {
  background: rgba(59, 130, 246, 0.12);
  color: #60a5fa;
  border: 2rpx solid rgba(59, 130, 246, 0.3);
}

.day-tag {
  margin-top: 2rpx;
  font-size: 18rpx;
  line-height: 1;
  font-weight: 700;
}

.calendar-day.disabled {
  color: #374151;
  background: transparent;
}

.calendar-legend {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx 24rpx;
  margin-top: 24rpx;
  color: #94a3b8;
  font-size: 22rpx;
}

.calendar-legend view {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.legend-dot {
  width: 18rpx;
  height: 18rpx;
  border-radius: 50%;
  display: inline-block;
}

.checked-dot {
  background: #F43F7A;
}

.pending-dot {
  background: #fbbf24;
}

.makeup-dot {
  background: #60a5fa;
}

.missed-dot {
  background: #f87171;
}

.checkin-card {
  border: none;
  background: linear-gradient(180deg, rgba(244, 63, 122, 0.08) 0%, #111522 40%);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.24);
}

.quick-photo {
  display: flex;
  align-items: center;
  padding: 32rpx 28rpx;
  background: linear-gradient(135deg, rgba(251, 191, 36, 0.12) 0%, rgba(245, 158, 11, 0.08) 100%);
  border-radius: 20rpx;
  margin-bottom: 8rpx;
  position: relative;
  overflow: hidden;
  border: 2rpx solid rgba(251, 191, 36, 0.15);
}

.quick-photo::after {
  content: '📸';
  position: absolute;
  right: -20rpx;
  bottom: -30rpx;
  font-size: 120rpx;
  opacity: 0.12;
}

.quick-photo-icon {
  font-size: 64rpx;
  margin-right: 24rpx;
  position: relative;
  z-index: 1;
}

.quick-photo-text {
  flex: 1;
  position: relative;
  z-index: 1;
}

.quick-photo-text .font-bold {
  color: #fbbf24;
  font-size: 32rpx;
}

.quick-photo-arrow {
  font-size: 40rpx;
  color: #fbbf24;
  font-weight: bold;
  position: relative;
  z-index: 1;
}

.btn-large {
  height: 96rpx;
  line-height: 96rpx;
  font-size: 32rpx;
  border-radius: 48rpx;
}

.login-prompt-card {
  border: none;
  background: linear-gradient(180deg, rgba(244, 63, 122, 0.08) 0%, #111522 50%);
}

.reminder-card {
  border: 2rpx solid #1F2937;
  box-shadow: 0 6rpx 24rpx rgba(0, 0, 0, 0.2);
}

.reminder-main {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
}

.reminder-title {
  font-size: 30rpx;
  color: #f8fafc;
  font-weight: 700;
}

.reminder-desc {
  margin-top: 8rpx;
  color: #94a3b8;
  font-size: 24rpx;
  line-height: 1.4;
}

.reminder-time-row {
  margin-top: 24rpx;
  padding: 20rpx 24rpx;
  background: #090B12;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  color: #94a3b8;
  font-size: 26rpx;
  font-weight: 600;
  border: 2rpx solid #1F2937;
}

.reminder-time-row text:last-child {
  color: #F43F7A;
}

.reminder-tip {
  margin-top: 16rpx;
  color: #64748b;
  font-size: 22rpx;
  line-height: 1.4;
}

.ranking-entry {
  cursor: pointer;
  transition: all 0.2s;
}

.ranking-entry:active {
  transform: scale(0.98);
  opacity: 0.8;
}

.badge-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
}

.badge-item {
  width: calc(25% - 15rpx);
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx 12rpx;
  background: #090B12;
  border-radius: 16rpx;
  opacity: 0.5;
  filter: grayscale(100%);
  transition: all 0.3s;
  border: 2rpx solid #1F2937;
}

.badge-item.achieved {
  opacity: 1;
  filter: none;
  background: linear-gradient(135deg, rgba(251, 191, 36, 0.12) 0%, rgba(245, 158, 11, 0.08) 100%);
  box-shadow: 0 4rpx 12rpx rgba(251, 191, 36, 0.12);
  border: 2rpx solid rgba(251, 191, 36, 0.2);
}

.badge-icon {
  font-size: 48rpx;
  margin-bottom: 8rpx;
}

.badge-name {
  font-size: 22rpx;
  color: #f8fafc;
  font-weight: 500;
  text-align: center;
  line-height: 1.3;
}

.badge-target {
  font-size: 20rpx;
  color: #64748b;
  margin-top: 4rpx;
}

.badge-item.achieved .badge-target {
  color: #fbbf24;
}

.material-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.material-item {
  display: flex;
  align-items: center;
  padding: 24rpx;
  background: #090B12;
  border-radius: 16rpx;
  transition: all 0.2s;
  border: 2rpx solid #1F2937;
}

.material-item:active {
  transform: scale(0.98);
  background: #111522;
  border-color: #374151;
}

.material-icon {
  width: 72rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #111522;
  border-radius: 16rpx;
  font-size: 36rpx;
  margin-right: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
  border: 2rpx solid #1F2937;
}

.material-info {
  flex: 1;
  min-width: 0;
}

.material-title {
  font-size: 28rpx;
  font-weight: 500;
  color: #f8fafc;
  line-height: 1.4;
}

.material-desc {
  font-size: 24rpx;
  color: #64748b;
  margin-top: 6rpx;
  line-height: 1.4;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.material-arrow {
  font-size: 28rpx;
  color: #64748b;
  margin-left: 12rpx;
}

.feed-card {
  padding-bottom: 20rpx;
}

.feed-list {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.feed-item {
  display: flex;
  align-items: flex-start;
  gap: 18rpx;
  padding: 22rpx 0;
  border-top: 2rpx solid #1F2937;
}

.feed-item:first-child {
  border-top: none;
  padding-top: 4rpx;
}

.feed-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #1F2937, #111522);
  color: #94a3b8;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  font-weight: 700;
  flex-shrink: 0;
  border: 2rpx solid #374151;
  overflow: hidden;
}

.feed-avatar-img {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.feed-body {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.feed-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16rpx;
}

.feed-name-row {
  display: flex;
  align-items: center;
  gap: 10rpx;
  min-width: 0;
  flex: 1;
}

.feed-name {
  font-size: 28rpx;
  color: #f8fafc;
  font-weight: 700;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.feed-date {
  color: #64748b;
  font-size: 22rpx;
  flex-shrink: 0;
  margin-top: 4rpx;
}

.feed-note {
  color: #94a3b8;
  font-size: 26rpx;
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.feed-image {
  width: 280rpx;
  max-width: 70%;
  border-radius: 16rpx;
  background: #090B12;
  display: block;
}

.feed-video {
  width: 100%;
  height: 320rpx;
  border-radius: 16rpx;
  background: #090B12;
}

.feed-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 12rpx;
  margin-top: 4rpx;
}

.feed-tag {
  display: inline-flex;
  padding: 2rpx 10rpx;
  border-radius: 999rpx;
  background: rgba(251, 191, 36, 0.12);
  color: #fbbf24;
  font-size: 18rpx;
  font-weight: 700;
  border: 1rpx solid rgba(251, 191, 36, 0.2);
  flex-shrink: 0;
}

.feed-like {
  margin: 0;
  min-width: 96rpx;
  height: 42rpx;
  padding: 0 16rpx;
  border-radius: 999rpx;
  background: #090B12;
  color: #64748b;
  font-size: 22rpx;
  font-weight: 700;
  line-height: 42rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  border: 1rpx solid #1F2937;
}

.feed-like.liked {
  background: rgba(244, 63, 122, 0.12);
  color: #F43F7A;
  border: 1rpx solid rgba(244, 63, 122, 0.2);
}

.feed-like::after {
  border: none;
}

.detail-sticky-action {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 100;
  display: flex;
  gap: 16rpx;
  padding: 18rpx 28rpx calc(18rpx + env(safe-area-inset-bottom));
  background: rgba(9, 11, 18, 0.96);
  border-top: 2rpx solid #1F2937;
  box-shadow: 0 -8rpx 28rpx rgba(0, 0, 0, 0.32);
  backdrop-filter: blur(18px);
}

.sticky-primary,
.sticky-secondary,
.sticky-rank {
  margin: 0;
  height: 82rpx;
  border-radius: 18rpx;
  font-size: 28rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
}

.sticky-primary,
.sticky-secondary {
  flex: 1;
}

.sticky-primary {
  background: linear-gradient(135deg, #F43F7A 0%, #ec4899 100%);
  color: #ffffff;
  box-shadow: 0 8rpx 20rpx rgba(244, 63, 122, 0.22);
}

.sticky-secondary {
  background: rgba(52, 211, 153, 0.12);
  color: #34d399;
  border: 1rpx solid rgba(52, 211, 153, 0.2);
}

.sticky-rank {
  width: 168rpx;
  background: #111522;
  color: #94a3b8;
  border: 2rpx solid #1F2937;
}

.sticky-primary::after,
.sticky-secondary::after,
.sticky-rank::after {
  border: none;
}

.poster-canvas {
  position: fixed;
  left: -9999rpx;
  top: 0;
  width: 640px;
  height: 1100px;
}

/* 状态横幅 */
.status-banner {
  display: flex;
  align-items: center;
  gap: 24rpx;
  padding: 28rpx 32rpx;
  border-radius: 20rpx;
  margin-bottom: 28rpx;
}

.status-banner.upcoming {
  background: rgba(139, 92, 246, 0.1);
  border: 2rpx solid rgba(139, 92, 246, 0.2);
}

.status-banner.expired {
  background: rgba(100, 116, 139, 0.1);
  border: 2rpx solid rgba(100, 116, 139, 0.2);
}

.status-banner-icon {
  font-size: 48rpx;
  flex-shrink: 0;
}

.status-banner-content {
  flex: 1;
}

.status-banner-title {
  font-size: 30rpx;
  font-weight: 700;
}

.status-banner.upcoming .status-banner-title {
  color: #a78bfa;
}

.status-banner.expired .status-banner-title {
  color: #94a3b8;
}

.status-banner-desc {
  font-size: 24rpx;
  margin-top: 6rpx;
  line-height: 1.5;
}

.status-banner.upcoming .status-banner-desc {
  color: #c4b5fd;
}

.status-banner.expired .status-banner-desc {
  color: #64748b;
}

/* 补充孩子名弹窗 */
.modal-mask {
  position: fixed;
  inset: 0;
  z-index: 1000;
  background: rgba(0, 0, 0, 0.72);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40rpx;
}

.modal-content {
  width: 100%;
  max-width: 560rpx;
  background: #111522;
  border-radius: 28rpx;
  padding: 40rpx 32rpx;
  border: 2rpx solid #1F2937;
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.5);
}

.modal-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #f8fafc;
  text-align: center;
  margin-bottom: 16rpx;
}

.modal-desc {
  font-size: 26rpx;
  color: #94a3b8;
  line-height: 1.5;
  text-align: center;
  margin-bottom: 32rpx;
}

.modal-input {
  height: 88rpx;
  background: #090B12;
  border: 2rpx solid #1F2937;
  border-radius: 16rpx;
  padding: 0 24rpx;
  color: #f8fafc;
  font-size: 30rpx;
  margin-bottom: 32rpx;
}

.modal-input::placeholder {
  color: #64748b;
}

.modal-actions {
  display: flex;
  gap: 20rpx;
}

.modal-btn {
  flex: 1;
  margin: 0;
  height: 80rpx;
  line-height: 80rpx;
  border-radius: 16rpx;
  font-size: 30rpx;
  font-weight: 600;
  text-align: center;
}

.modal-btn::after {
  border: none;
}

.modal-btn-secondary {
  background: #1F2937;
  color: #cbd5e1;
}

.modal-btn-primary {
  background: linear-gradient(135deg, #F43F7A 0%, #ec4899 100%);
  color: white;
}

.success-content {
  padding: 48rpx 36rpx 36rpx;
}

.success-actions {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin-top: 8rpx;
}

.success-btn {
  width: 100%;
  margin: 0;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 16rpx;
  font-size: 30rpx;
  font-weight: 600;
  text-align: center;
}

.success-btn::after {
  border: none;
}

.success-btn-primary {
  background: linear-gradient(135deg, #F43F7A 0%, #ec4899 100%);
  color: white;
  box-shadow: 0 8rpx 24rpx rgba(244, 63, 122, 0.25);
}

.success-btn-share {
  background: #111522;
  color: #f8fafc;
  border: 2rpx solid #F43F7A;
}

.success-close {
  margin-top: 28rpx;
  text-align: center;
  font-size: 26rpx;
  color: #64748b;
}
