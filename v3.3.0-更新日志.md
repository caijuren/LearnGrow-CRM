# 源来是糖 v3.3.0 更新日志

## 一、UI 界面优化

### 1. 全局视觉统一
- 统一所有页面背景色为深色主题 `#090B12`，修复部分页面下拉刷新时露出浅色背景的问题。
- 统一导航栏颜色，隐私政策页等页面与整体深色风格保持一致。
- 完善设计令牌体系，新增 `--success-dark`、`--warning-dark`、`--accent-light`、`--accent-lighter` 等变量，进一步减少硬编码色值。

### 2. 登录页优化
- 修复微信登录按钮在点击/加载时文字断行的问题。
- 优化头像选择区域：使用更清晰的图标与文案提示，选中后显示编辑角标。
- 优化昵称、孩子名称输入框，增加明确标签，视觉更聚焦。
- 同意隐私协议复选框样式与深色主题统一。

### 3. 个人中心优化
- 编辑头像增加玫瑰红编辑角标，与登录页头像选择风格一致。
- 统计数字颜色统一使用语义化变量。

### 4. 首页优化
- 今日打卡圆环、活动卡片、进度条颜色统一使用设计令牌。
- 进度条宽度改为 JS 计算后绑定，减少 WXML 内联计算。

### 5. 活动详情页优化
- 状态横幅、连续打卡数字、底部操作按钮颜色统一。
- 打卡成功提示移除 emoji，改为纯文字提示。
- 上传图片/视频区域、日期选择等细节颜色统一。

### 6. 排行榜优化
- 领奖台前三名图标与颜色更醒目。
- 我的排名卡片增加头像展示，信息层级更清晰。
- 列表增加「全部排名」小标题，空状态文案更友好。

### 7. 我的打卡页优化
- 已结束 badge 颜色修复为深色主题下的弱化色。
- 统计数字、日历格子、近期记录颜色统一。

### 8. 隐私政策页优化
- 增加页面标题区与「最后更新日期」说明。
- 章节标题增加玫瑰红左边框，内容卡片复用全局 `.card` 样式。

## 二、交互与逻辑优化

### 1. 错误提示完善
- 清理所有页面 JS 中的调试 `console.log` / `console.error`。
- 在首页、排行榜、我的打卡、个人中心、登录页、活动详情页等关键请求失败处，统一补充 `wx.showToast` 用户可感知提示，避免空白页。

### 2. 代码质量
- 修复排行榜领奖台第三名重复的 `wx:if` 条件判断。
- 首页活动卡片进度条改为在 JS 中计算 `progressPercent`。
- 个人中心编辑头像增加编辑角标提示。

### 3. 多媒体打卡
- 打卡已支持上传图片和视频，用户可在活动详情页选择图片或录制视频进行打卡。

## 三、版本信息

- 小程序版本号：**3.3.0**
- 后端接口版本：兼容当前生产环境
- 主要影响范围：小程序端 UI 与交互体验，无破坏性接口变更

## 四、登录审核问题修复

针对审核反馈「小程序打开【登录】报错」的问题，已进行以下修复：
- 前端登录请求改为由登录页统一展示错误提示，避免与通用请求层重复弹窗。
- 前端错误提示现在会展示后端返回的具体原因（如「微信登录配置缺失」「invalid code」等），便于快速定位问题。
- 后端 `/api/wx/login` 增加对微信 `jscode2session` 响应与异常的详细日志记录。

> 已确认服务器 `/var/www/learngrow-crm/.env.production` 中的 `WX_APPID` 和 `WX_SECRET` 已配置为当前小程序的真实值，并已完成后端部署。

## 五、审核说明

本次更新为 UI 一致性优化与体验提升，未新增用户隐私权限申请，未变更登录流程与授权范围。登录仍为一键微信登录，用户可在未登录状态下浏览活动列表与排行榜，仅在参与打卡时需要登录，符合微信小程序审核规范。
