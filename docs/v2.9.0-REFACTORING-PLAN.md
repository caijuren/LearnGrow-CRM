# v2.9.0 架构模块化重构方案

**执行时间:** 2026-09-15 ~ 09-30 (16天)
**当前状态:** 规划阶段

---

## 📋 重构目标

将当前3207行的单体 `api/app.ts` 文件拆分为清晰的三层架构：

```
api/
├── routes/           # 路由层 - 处理HTTP请求/响应
├── services/         # 服务层 - 业务逻辑封装
├── repositories/     # 仓储层 - 数据访问抽象
├── middleware/       # 中间件 - 认证/限流/错误处理
└── app.ts            # 应用入口 - 仅负责插件注册和路由挂载
```

---

## 🎯 拆分策略

### 阶段1: 路由层拆分 (第1-4天)

**目标结构:**
```
api/routes/
├── auth.routes.ts        # 认证相关 (~100行)
├── wxUser.routes.ts      # 微信用户 (~400行)
├── checkin.routes.ts     # 打卡相关 (~600行)
├── order.routes.ts       # 订单相关 (~300行)
├── dashboard.routes.ts   # 驾驶舱 (~200行)
├── product.routes.ts     # 产品管理 (~150行)
├── child.routes.ts       # 孩子档案 (~150行)
├── group.routes.ts       # 微信群 (~200行)
├── material.routes.ts    # 素材库 (~150行)
├── settings.routes.ts    # 系统配置 (~100行)
├── backup.routes.ts      # 备份恢复 (~150行)
├── live.routes.ts        # 直播互动 (~200行)
├── user-delete.routes.ts # 用户删除 (已有)
└── index.ts              # 统一注册入口
```

**拆分示例 (auth.routes.ts):**

```typescript
import { FastifyInstance, FastifyReply, FastifyRequest } from 'fastify';
import bcrypt from 'bcryptjs';
import { z } from 'zod';
import db from '../db.js';
import { authMiddleware, type AuthUser } from '../services/auth.service.js';

const ADMIN_LOGIN_WINDOW_MS = 15 * 60 * 1000;
const ADMIN_LOGIN_MAX_ATTEMPTS = 10;
const adminLoginAttempts = new Map<string, { count: number; resetAt: number }>();

function allowAdminLogin(request: FastifyRequest, reply: FastifyReply): boolean {
  const ip = request.ip;
  const now = Date.now();
  const current = adminLoginAttempts.get(ip);

  if (!current || current.resetAt <= now) {
    adminLoginAttempts.set(ip, { count: 1, resetAt: now + ADMIN_LOGIN_WINDOW_MS });
    return true;
  }

  if (current.count >= ADMIN_LOGIN_MAX_ATTEMPTS) {
    reply.code(429).send({ success: false, error: '登录尝试次数过多，请稍后重试' });
    return false;
  }

  current.count += 1;
  return true;
}

export async function registerAuthRoutes(app: FastifyInstance) {
  // POST /api/auth/login
  app.post('/api/auth/login', {
    schema: {
      tags: ['认证'],
      summary: '管理端登录',
      body: {
        type: 'object',
        required: ['username', 'password'],
        properties: {
          username: { type: 'string' },
          password: { type: 'string', format: 'password' }
        }
      },
      response: {
        200: { /* ... */ },
        401: { /* ... */ }
      }
    }
  }, async (request, reply) => {
    if (!allowAdminLogin(request, reply)) return;

    const parsed = z.object({
      username: z.string().min(1),
      password: z.string().min(1)
    }).safeParse(request.body);

    if (!parsed.success) {
      return reply.code(400).send({
        success: false,
        error: '用户名和密码不能为空'
      });
    }

    const { username, password } = parsed.data;
    const user = db.prepare('SELECT * FROM users WHERE username = ?').get(username) as any;

    if (!user) {
      return reply.code(401).send({
        success: false,
        error: '用户名或密码错误'
      });
    }

    if (!bcrypt.compareSync(password, user.password)) {
      return reply.code(401).send({
        success: false,
        error: '用户名或密码错误'
      });
    }

    const token = app.jwt.sign({
      id: user.id,
      username: user.username,
      role: user.role
    });

    return {
      success: true,
      data: {
        token,
        user: {
          id: user.id,
          username: user.username,
          role: user.role,
          display_name: user.display_name
        }
      }
    };
  });

  // GET /api/auth/me
  app.get('/api/auth/me', {
    preHandler: [authMiddleware]
  }, async (request, reply) => {
    const user = db.prepare(
      'SELECT id, username, role, display_name, created_at FROM users WHERE id = ?'
    ).get((request.user as AuthUser).id) as any;

    if (!user) {
      return reply.code(404).send({
        success: false,
        error: '用户不存在'
      });
    }

    return { success: true, data: user };
  });
}
```

### 阶段2: 服务层抽取 (第5-8天)

**目标结构:**
```
api/services/
├── auth.service.ts       # JWT生成/验证、登录限流
├── checkin.service.ts    # 打卡统计、排名计算
├── points.service.ts     # 积分计算、流水记录
├── backup.service.ts     # 备份创建/恢复 (已有)
├── user-delete.service.ts # 用户删除 (已有)
├── wx-share.service.ts   # 分享链接生成 (已有)
├── stats.service.ts      # 统计数据聚合
└── reminder.service.ts   # 打卡提醒任务
```

**服务层示例 (checkin.service.ts):**

```typescript
import db from '../db.js';
import type { CheckinEvent, CheckinRecord } from '../../shared/types.js';

export interface CheckinStats {
  totalParticipants: number;
  totalRecords: number;
  approvedRecords: number;
  pendingRecords: number;
  activeParticipants7d: number;
}

/**
 * 获取打卡活动统计信息
 */
export async function getCheckinStats(eventId: number): Promise<CheckinStats> {
  const totalParticipants = db.prepare(`
    SELECT COUNT(*) as count
    FROM checkin_participants
    WHERE event_id = ? AND status = 'active'
  `).get(eventId) as { count: number };

  const totalRecords = db.prepare(`
    SELECT COUNT(*) as count
    FROM checkin_records r
    JOIN checkin_participants p ON r.participant_id = p.id
    WHERE p.event_id = ?
  `).get(eventId) as { count: number };

  const approvedRecords = db.prepare(`
    SELECT COUNT(*) as count
    FROM checkin_records r
    JOIN checkin_participants p ON r.participant_id = p.id
    WHERE p.event_id = ? AND r.status = 'approved'
  `).get(eventId) as { count: number };

  const pendingRecords = db.prepare(`
    SELECT COUNT(*) as count
    FROM checkin_records r
    JOIN checkin_participants p ON r.participant_id = p.id
    WHERE p.event_id = ? AND r.status = 'pending'
  `).get(eventId) as { count: number };

  const activeParticipants7d = db.prepare(`
    SELECT COUNT(DISTINCT p.wx_user_id) as count
    FROM checkin_participants p
    JOIN checkin_records r ON r.participant_id = p.id AND r.status = 'approved'
    WHERE p.event_id = ? AND r.checkin_date >= date('now', '-7 days')
  `).get(eventId) as { count: number };

  return {
    totalParticipants: totalParticipants.count,
    totalRecords: totalRecords.count,
    approvedRecords: approvedRecords.count,
    pendingRecords: pendingRecords.count,
    activeParticipants7d: activeParticipants7d.count,
  };
}

/**
 * 获取打卡排行榜
 */
export function getCheckinRanking(eventId: number, limit = 50) {
  return db.prepare(`
    SELECT
      p.wx_user_id,
      u.nickname,
      u.avatar_url,
      COUNT(r.id) as record_count,
      SUM(CASE WHEN r.status = 'approved' THEN 1 ELSE 0 END) as approved_count
    FROM checkin_participants p
    JOIN wx_users u ON p.wx_user_id = u.id
    LEFT JOIN checkin_records r ON r.participant_id = p.id
    WHERE p.event_id = ? AND p.status = 'active'
    GROUP BY p.wx_user_id
    ORDER BY approved_count DESC, record_count DESC
    LIMIT ?
  `).all(eventId, limit) as any[];
}
```

### 阶段3: 仓储层封装 (第9-11天)

**目标结构:**
```
api/repositories/
├── base.repo.ts          # 通用CRUD方法
├── wxUser.repo.ts        # 微信用户数据访问
├── checkin.repo.ts       # 打卡数据访问
├── order.repo.ts         # 订单数据访问
├── product.repo.ts       # 产品数据访问
└── child.repo.ts         # 孩子档案数据访问
```

**仓储层示例 (base.repo.ts):**

```typescript
import { Database } from 'better-sqlite3';
import db from '../db.js';

export interface BaseRepository<T extends { id: number }> {
  findById(id: number): T | undefined;
  findAll(options?: ListOptions): T[];
  create(data: Partial<T>): number;
  update(id: number, data: Partial<T>): void;
  delete(id: number): void;
}

export interface ListOptions {
  page?: number;
  limit?: number;
  sortBy?: string;
  order?: 'ASC' | 'DESC';
  where?: string;
  params?: any[];
}

export class BaseRepositoryImpl<T extends { id: number }> implements BaseRepository<T> {
  protected tableName: string;
  protected db: Database;

  constructor(tableName: string) {
    this.tableName = tableName;
    this.db = db;
  }

  findById(id: number): T | undefined {
    return this.db.prepare(`SELECT * FROM ${this.tableName} WHERE id = ?`).get(id) as T | undefined;
  }

  findAll(options: ListOptions = {}): T[] {
    const {
      page = 1,
      limit = 20,
      sortBy = 'created_at',
      order = 'DESC',
      where = '',
      params = []
    } = options;

    let sql = `SELECT * FROM ${this.tableName}`;
    if (where) sql += ` WHERE ${where}`;
    sql += ` ORDER BY ${sortBy} ${order}`;
    sql += ` LIMIT ${limit} OFFSET ${(page - 1) * limit}`;

    return this.db.prepare(sql).all(...params) as T[];
  }

  create(data: Partial<T>): number {
    const columns = Object.keys(data).join(', ');
    const placeholders = Object.keys(data).map(() => '?').join(', ');
    const values = Object.values(data);

    const result = this.db.prepare(
      `INSERT INTO ${this.tableName} (${columns}) VALUES (${placeholders})`
    ).run(...values);

    return result.lastInsertRowid as number;
  }

  update(id: number, data: Partial<T>): void {
    const fields = Object.keys(data).map(key => `${key} = ?`).join(', ');
    const values = [...Object.values(data), id];

    this.db.prepare(
      `UPDATE ${this.tableName} SET ${fields} WHERE id = ?`
    ).run(...values);
  }

  delete(id: number): void {
    this.db.prepare(`DELETE FROM ${this.tableName} WHERE id = ?`).run(id);
  }
}
```

**使用示例 (wxUser.repo.ts):**

```typescript
import { BaseRepositoryImpl, ListOptions } from './base.repo.js';
import type { WxUser } from '../../shared/types.js';

export class WxUserRepository extends BaseRepositoryImpl<WxUser> {
  constructor() {
    super('wx_users');
  }

  findByOpenid(openid: string): WxUser | undefined {
    return this.db.prepare(
      'SELECT * FROM wx_users WHERE openid = ?'
    ).get(openid) as WxUser | undefined;
  }

  findByPhone(phone: string): WxUser | undefined {
    return this.db.prepare(
      'SELECT * FROM wx_users WHERE phone = ?'
    ).get(phone) as WxUser | undefined;
  }

  search(keyword: string, options: ListOptions = {}): WxUser[] {
    const where = '(nickname LIKE ? OR name LIKE ? OR phone LIKE ?)';
    const params = [`%${keyword}%`, `%${keyword}%`, `%${keyword}%`];

    return this.findAll({
      ...options,
      where,
      params
    });
  }

  updateStage(userId: number, stage: string): void {
    this.update(userId, { stage });
  }
}

export const wxUserRepo = new WxUserRepository();
```

### 阶段4: 中间件整理 (第12-13天)

**目标结构:**
```
api/middleware/
├── auth.middleware.ts    # JWT验证、权限检查
├── rateLimit.middleware.ts # 请求限流
├── errorHandler.middleware.ts # 全局错误处理
├── validator.middleware.ts # 请求体验证
└── logger.middleware.ts  # 访问日志
```

**中间件示例 (auth.middleware.ts):**

```typescript
import { FastifyRequest, FastifyReply, done } from 'fastify';
import jwt from '@fastify/jwt';
import { JWT_SECRET } from '../config/constants.js';

export interface AuthUser {
  id: number;
  username: string;
  role: 'admin' | 'operator' | 'viewer';
}

export async function authMiddleware(
  request: FastifyRequest,
  reply: FastifyReply
) {
  try {
    await request.jwtVerify();
  } catch (err) {
    return reply.code(401).send({
      success: false,
      error: '登录已过期，请重新登录'
    });
  }
}

export async function adminOnly(
  request: FastifyRequest,
  reply: FastifyReply
) {
  await authMiddleware(request, reply);

  const user = request.user as AuthUser;
  if (user.role !== 'admin') {
    return reply.code(403).send({
      success: false,
      error: '权限不足，需要管理员权限'
    });
  }
}

export async function operatorOrAbove(
  request: FastifyRequest,
  reply: FastifyReply
) {
  await authMiddleware(request, reply);

  const user = request.user as AuthUser;
  if (!['admin', 'operator'].includes(user.role)) {
    return reply.code(403).send({
      success: false,
      error: '权限不足，需要运营人员及以上权限'
    });
  }
}
```

### 阶段5: TypeScript严格模式 (第14-16天)

**修改 tsconfig.json:**

```json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
```

**修复典型类型错误:**

```typescript
// ❌ Before
const user = db.prepare('SELECT * FROM users WHERE id = ?').get(id);
console.log(user.name); // user可能为undefined

// ✅ After
const user = db.prepare('SELECT * FROM users WHERE id = ?').get(id) as User | undefined;
if (!user) {
  throw new Error('用户不存在');
}
console.log(user.name); // TypeScript知道user不为undefined
```

---

## 🔄 迁移步骤

### 第1步: 创建新目录结构
```bash
mkdir -p api/{routes,services,repositories,middleware}
```

### 第2步: 逐个拆分路由
1. 从app.ts中复制相关代码到新路由文件
2. 修改导入路径
3. 导出注册函数
4. 在app.ts中替换为路由注册调用

### 第3步: 抽取服务层
1. 识别业务逻辑代码（统计计算、数据处理等）
2. 移动到services目录
3. 路由层仅保留请求/响应处理

### 第4步: 封装仓储层
1. 将所有db.prepare调用移到repository
2. 路由和服务层通过repository访问数据

### 第5步: 整理中间件
1. 将app.ts中的中间件函数移到middleware目录
2. 统一导出和导入路径

### 第6步: 开启严格模式
1. 修改tsconfig.json
2. 运行 `npm run check` 查看错误
3. 逐个修复类型问题

### 第7步: 回归测试
```bash
npm test
npm run build
# 手动验证核心功能
```

---

## ⚠️ 风险控制

1. **每步都提交git**: 每个阶段完成后立即提交
2. **保留原app.ts备份**: 创建 `app.ts.backup`
3. **渐进式迁移**: 先迁移非核心模块（如settings），验证后再迁移核心模块
4. **自动化测试**: 每迁移一个模块就运行一次测试
5. **回滚预案**: 如遇严重问题，可快速回滚到backup版本

---

## 📊 验收标准

- [ ] `api/app.ts` 文件大小 < 200行
- [ ] 单个文件最大行数 < 500行
- [ ] 无循环依赖（使用 `dependency-cruiser` 检测）
- [ ] `npm run check` 无类型错误
- [ ] 所有集成测试通过
- [ ] 本地手动验证核心功能正常

---

## 📝 下一步行动

1. **召开kickoff会议**: 确认重构范围和优先级
2. **创建GitHub Milestone**: v2.9.0包含的所有Issue
3. **分配负责人**: 后端开发主导，前端配合测试
4. **开始阶段1**: 路由层拆分

**预计开始时间:** 2026-09-15
**预计完成时间:** 2026-09-30
